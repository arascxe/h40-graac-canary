# FEE100K incident checkpoint — 2026-09-26 17:14 UTC

## Scope and invariants

This checkpoint continues the immutable prospective research state without changing production jobs, cron, schema, alert routing, thresholds, freezes, cohorts, or financial state. No paid service, mint, wallet signature, transfer, trade, or irreversible action was performed. Old CSR/raw CTO/trader-side research was not opened.

The main 20, their separately matched controls, the separate discovery cohort, and the frozen 210 remain unchanged. Missing coverage is `DATA_GAP`, never a negative economic result.

## Fail-fast database gate

Supabase project `iocirjhwncnhjanawgsm` reports `ACTIVE_HEALTHY`, but the independent log plane shows an active multi-family database incident through the end of the inspected window.

Window: 2026-09-26 15:45:00–17:14:00 UTC.

- 117 SQLSTATE `57014` statement timeouts.
  - 96 were pg_cron work; 21 were `postgres_exporter` probes.
- 52 SQLSTATE `08006` connection failures.
- 21 SQLSTATE `08P01` protocol/connection failures.
- First error: 15:45:30 UTC.
- Last observed error: 17:13:33 UTC.

pg_cron timeout families:

| Family | 57014 count | Last seen UTC |
|---|---:|---|
| `discovery_adapter_bus_tick_v2()` | 37 | 17:13:06 |
| `process_cycle()` | 28 | 17:13:14 |
| `turnover_outcome_probe_tick()` | 10 | 17:10:41 |
| `turnover_research_maintenance()` | 7 | 16:59:06 |
| premint refresh pair | 6 | 16:59:06 |
| `crypto_crossover_tick_v1()` | 4 | 17:08:02 |
| `forensic_maintenance()` | 1 | 16:13:06 |
| `refresh_family_lane_guarded()` | 1 | 16:25:01 |
| `retention()` | 1 | 16:39:02 |
| unclassified pg_cron | 1 | 16:05:59 |

Representative current failures contend on `fee100k_http_request_v1`, exact external-URL token-link refresh, and crypto crossover inserts. This is evidence of broad resource/lock pressure, not proof of one root function.

Per the fail-fast rule, this session sent **zero PostgreSQL SQL**: no size probe, cohort query, `EXPLAIN`, full count, job rerun, or manual compaction. Current database capacity is therefore unknown; the last durable measurement remains 412,036,243 bytes / 500 MB (82.41%) at 2026-09-25 08:19 UTC.

## GitHub Actions and source coverage

### Crypto-native crossover

Run: https://github.com/arascxe/h40-graac-canary/actions/runs/36250897182

Technical workflow conclusion: success. Economic/coverage conclusion: not validated.

- Snapshot generated: 2026-09-26 15:09:02 UTC.
- Jetstream: 80,000 messages; cursor 2026-09-25 14:13:11–14:36:48 UTC.
- Cursor lag at publication: about 24h 32m.
- `caught_up_near_live=false`.
- `coverage_status=PARTIAL_OR_BASELINE_UNKNOWN`.
- Fresh keyword posts: 0.
- Reddit lanes: explicit approval required; no unauthorized retry.
- Bluesky search: disabled after 403; Jetstream fallback is not a coverage success.
- Mastodon produced three same-actor, spam-prone tag observations.

Manual exact-object audit of all three published observations:

- `linked_object_urls=[]`
- `has_exact_outbound_object=false`
- no independently sourced pre-mint object URL
- no candidate requiring mint-time comparison
- no pilot

Therefore exact-object crossover remains `DATA_GAP`, not zero economic demand.

### Discovery, propagation, and TikTok

Live data branches published during this check:

- Discovery at 17:15:12 UTC: 49 items, 40 independent actor hashes, 43 items with platform links.
- Propagation at 17:14:38 UTC: 114 items, 108 independent actor hashes, 103 items with platform links.
- TikTok at 17:14:40 UTC: two hashtags and **zero videos**.

These are source observations, not cohort outcomes, verified pre-mint crossovers, pilots, or economic validation. TikTok video coverage remains absent.

### MDRTF

Completed/cancelled run: https://github.com/arascxe/h40-graac-canary/actions/runs/36237390508

- The run was cancelled after remaining in the 208-cut collection step.
- Runtime health failed with `Operational cadence failure`.
- Every inspected cut still reported the Pump realtime stream unavailable because the `aiohttp` transport could not be imported.
- The persisted artifact was 568,304,281 bytes.
- The replacement run https://github.com/arascxe/h40-graac-canary/actions/runs/36253784545 is still in the same collection step on unchanged commit `53be0685912b8b4ddc6a6bd1aa4f06f57a2704e0`; its start is not a pass.

The previously selected fail-before/pass-after `aiohttp` dependency repair remains the highest-information bounded development, but its execution is gated on (1) a clean database log window and (2) the current unchanged MDRTF run ending. Repeating or modifying the live workflow during the active incident would confound the diagnosis and add load.

## Economic evidence

No independently verified pre-mint exact-object crossover, no real pilot candidate, and no verified net creator-fee receipt to the user wallet were found. Volume, estimated fees, and wallet-verified net receipts remain separate evidence classes; none is promoted by this checkpoint.

## Session boundary

Automatic production services produced the source snapshots and workflow runs above. This session only read the independent Supabase log plane and GitHub state, manually audited the three crypto observations, enforced the database fail-fast gate, and wrote this checkpoint. It did not run or alter production work.
