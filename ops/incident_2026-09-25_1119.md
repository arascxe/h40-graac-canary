# FEE100K multi-family database and scheduler incident — 2026-09-25 11:19 UTC

Scope: Supabase independent log plane plus GitHub Actions metadata/logs. No PostgreSQL SQL, EXPLAIN, table scan, job rerun, workflow dispatch, production mutation, cohort query, or freeze change was performed.

## Database incident

Supabase project status remained `ACTIVE_HEALTHY`, but the data plane entered a broad incident between 10:38 and 11:19 UTC:

- 148 pg_cron job-startup timeouts, first 10:38:04 and still occurring at 11:18:45 UTC.
- 48 SQLSTATE `57014` statement timeouts, first 10:43:41 and still occurring at 11:18:30 UTC.
- 34 connection-level errors: 20 SQLSTATE `08006` resets plus 14 `08P01`/EOF protocol failures, first 10:39:11 and still occurring at 11:18:40 UTC.

Affected startup-timeout families:

| Job | Function | Count |
|---:|---|---:|
| 94 | `fast_freeze_exact_objects()` | 25 |
| 81 | `queue_sources()` | 21 |
| 98 | premint TikTok/discovery processing | 14 |
| 95 | `turnover_outcome_probe_tick()` | 13 |
| 100 | `discovery_adapter_bus_tick_v2()` | 12 |
| 103 | `crypto_crossover_tick_v1()` | 10 |
| 82 | `process_cycle()` | 8 |
| 87 / 97 / 106 | forensics, discovery queue, aux20 crossover | 6 each |
| 85 / 102 | watchdog, control freeze | 5 each |
| 91 / 99 | turnover maintenance, premint refresh/evidence | 4 each |
| 88 / 96 | forensic maintenance, compactor | 3 each |
| 101 | family lane | 2 |
| 83 | retention | 1 |

Statement-timeout families included `process_cycle()` (13), discovery adapter bus (13), premint TikTok/discovery (7), outcome probe (4), premint refresh/evidence (3), turnover maintenance (2), and Supabase telemetry queries. Logged contexts include shared HTTP-response updates, object-token link refresh, propagation guard, premint seed calculations, and response reads.

This breadth establishes a shared database availability/contention incident. It does not establish one causal function. All affected scientific outputs during the window are `DATA_GAP`, not negative results.

## GitHub Actions and MDRTF

No repository Actions run was created after crypto run 36101180099 at 06:03 UTC through 11:19 UTC, despite the crypto workflow declaring a 15-minute schedule.

MDRTF run 36092388913 ended `cancelled` at 11:04 UTC near its configured 355-minute job boundary. The `Freeze runtime health` step failed with `Operational cadence failure`. Logs repeatedly reported:

- `coverage_complete=false`
- decision `COVERAGE_INSUFFICIENT`
- Solana and other lane coverage false
- Pump realtime stream `STREAM_UNAVAILABLE`
- exact error: `ImportError: Could not import aiohttp transport`

The workflow installs `nats-py==2.11.0` but does not install `aiohttp`. Unit tests passed because they did not exercise the live transport dependency. This is a verified packaging/real-runtime coverage defect.

The failed run still uploaded a 565,638,843-byte `mdrtf-state` artifact with seven-day retention. Its contribution to GitHub scheduling suppression or quota pressure is plausible but unproven.

## Safe remediation gate

Do not edit the workflow during the active database incident: changing `.github/workflows/mdrtf-future-only.yml` would trigger another nearly six-hour run by its push path.

Before any production workflow change:

1. Add a dependency-presence smoke that imports `aiohttp` and instantiates the intended transport without network activity.
2. Pin a compatible `aiohttp` range and run existing stream/unit tests in an isolated branch or local environment.
3. Reduce or split the persisted artifact only after proving state-restoration compatibility and rollback; do not delete existing evidence.
4. Prevent a failed operational window from being called PASS merely because state upload succeeds.
5. Confirm Actions schedules resume naturally after the long run clears before dispatching anything manually.

Rollback for a dependency-only change is the prior workflow blob; no database, freeze, or economic state needs mutation.

## Scientific/economic state

Crypto cursor remains 2026-09-23 21:30:47.437 UTC, about 37h48m old at 11:19 UTC. Discovery's latest published snapshot remains 09:09 UTC; TikTok remains two hashtags and zero videos. Primary 20, matched controls, exploration cohort, and frozen 210 were not queried or altered.

Decision: `CRITICAL_MULTI_FAMILY_DB_INCIDENT / ACTIONS_SCHEDULER_GAP / MDRTF_OPERATIONAL_CADENCE_FAIL / AIOHTTP_RUNTIME_DEFECT_VERIFIED / DATA_GAP / NO_PRODUCTION_CHANGE`.

No independently verified pre-mint pilot, user-wallet creator-fee receipt, or revenue milestone exists.
